---
title: "前処理"
description: "分析する前に"
---
データの読み込みについて、標準ライブラリと外部ライブラリ（readr）があります。  
高速なのは外部ライブラリ（readr）です。
readrはtidyverseに入っているため、新たに入れる必要はありません。

## データの読み込み（標準ライブラリ）
read.拡張子名で読み込むことができる
```r
read.csv("csvファイル")

# 変数に入れることで加工をできるようにしておく。
df = read.csv("csvファイル")
```

## データの読み込み（外部ライブラリ:readr）
"."が"_"になっています。
```r
read_csv("csvファイル")
df = read_csv("csvファイル")
```
*ほかの形式を読み込みたいとき
SPSSやStata、SASを読み込みたいときは、havenパッケージ  
Excelを読み込みたいときはreadxlパッケージを使います。

## 型を確認したいとき
```r
str(データフレーム名)
```
もしくはRstdioの場合、自分でデータを変数名で定義すると、右上のタブで▽をクリックすると型と最初の10行を見ることができます。
## 名前の変更、型の変更をしたいとき
読み込まれた型を変更したいときはas_型名で可能。
```r

```
