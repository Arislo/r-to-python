---
title: "前処理"
description: "分析する前に"
---
# データの読み込み
read.拡張子名で読み込むことができる
```r
read.csv("csvファイル")

df = read.csv("csvファイル")
```
変数に入れることで加工をできるようにしておく。

# 名前の変更、型の変更をしたいとき
読み込まれた型を変更したいときはas_型名で可能。
```r

```
